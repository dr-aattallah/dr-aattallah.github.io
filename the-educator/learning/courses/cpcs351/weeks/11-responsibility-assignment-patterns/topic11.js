(()=>{const pages=[['index.html','Patterns & Responsibility'],['gof-singleton.html','GoF & Singleton'],['grasp-controller.html','GRASP & Controller'],['controller-quality.html','Controller Quality'],['expert.html','Expert Pattern'],['creator.html','Creator Pattern'],['worked-example.html','Patterns Working Together'],['review.html','Review & Practice']];const current=Number(document.body.dataset.page||1)-1;const side=document.getElementById('topic-sidebar'),mobile=document.getElementById('topic-mobile');const links=pages.map((p,i)=>`<a class="${i===current?'active':''}" href="${p[0]}"><span>${String(i+1).padStart(2,'0')}</span>${p[1]}</a>`).join('');if(side)side.innerHTML=`<div class="side-title"><small>Topic 11</small><strong>Responsibility Assignment Patterns</strong></div><nav class="side-nav">${links}</nav>`;if(mobile)mobile.innerHTML=`<select aria-label="Topic pages" onchange="location.href=this.value">${pages.map((p,i)=>`<option value="${p[0]}" ${i===current?'selected':''}>${i+1}. ${p[1]}</option>`).join('')}</select>`;const prev=document.getElementById('prev-link'),next=document.getElementById('next-link');if(prev&&current>0){prev.hidden=false;prev.href=pages[current-1][0];}if(next&&current<pages.length-1){next.hidden=false;next.href=pages[current+1][0];}})();